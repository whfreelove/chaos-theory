# Feature: Cross-capability integration (integration)

<!--
This change has a single capability (isolated-plugin-tests).
No cross-capability interactions exist, so no integration scenarios are needed.
-->

## Integration Scenarios

None. Single-capability change — all behaviors are covered by `requirements/isolated-plugin-tests/requirements.feature.md`.
