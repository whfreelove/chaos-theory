---
name: validate-specs
description: Use when OpenSpec artifacts need validation before proceeding. Use after creating or updating proposal.md, design.md, specs, or tasks.md.
---

# Artifact Validation

Validates OpenSpec artifacts through critique and resolution workflow.

## Workflow

1. Invoke `ct:critique-specs` skill
2. Invoke `ct:resolve-specs` skill
