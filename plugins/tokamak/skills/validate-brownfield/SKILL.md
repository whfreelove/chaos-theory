---
name: validate-brownfield
description: Validate brownfield documentation artifacts through critique and resolution. Use after creating or updating reverse-engineered documentation from existing code.
---

# Brownfield Documentation Validation

Validates brownfield documentation artifacts through critique (accuracy + consistency) and gap resolution.

## Workflow

1. Invoke `tokamak:critique-specs-brownfield` skill
2. Invoke `tokamak:resolve-gaps-brownfield` skill
